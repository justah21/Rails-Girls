<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag(@post.image, size: "70") %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
<h1> Comments </h1>
<%- @post.comments.each do |comment| %>
<b><%= comment.user %>:</b>
<br>
<%= comment.content %>
<br>
<% end %>
<div class="row">
  <div class="col-md-6">To jest przykładowy tekst</div>
  <div class="col-md-6">To jest inny tekst</div>

</div>
